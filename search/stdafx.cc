#include "stdafx.h"
// this is actually a C source file, not C++, but it needs a filename that
// distinguishes it from *.c; the build script that uses it to generate the
// precompiled headers, can instruct the compiler to treat it as C
